// Package exporter provides configuration import/export for Claude Code projects.
//
// It supports exporting .claude/ directories as base64-encoded ZIP archives
// and importing them back with path traversal protection. This package has no
// Wails dependency and can be used from both the MCP server and the desktop app.
package exporter
